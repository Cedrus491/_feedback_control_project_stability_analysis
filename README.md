# _feedback_control_project_stability_analysis
suggest test after download the file

1. First run `env_simulator.py`, then you can check out the folder named `simulator_results`.

2. Then run `stability_analysis.py`, you will get the folder `stability_results` with many figures.

3. Finally, run `robust_analysis.py`, and you will amazingly discover that there is a new figure in the `stability_results` folder.
